PORT=5000
DB_NAME=auth_db
DB_USER=root
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=3306
JWT_SECRET=your_secret_key
# For production (e.g. Render/Railway), use the full database URL if provided:
# DATABASE_URL=mysql://user:password@host:port/database
